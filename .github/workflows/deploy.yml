name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - dev
  push:
    branches:
      - dev
      - main

jobs:
  # 🧪 Job 1: Dev branch test with docker-compose.dev.yml
  test-dev:
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build & Run with docker-compose.dev.yml
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_DEV }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        run: |
          echo "NEON_API_KEY=${{ secrets.NEON_API_KEY }}" >> .env.development.local
          echo "NEON_PROJECT_ID=${{ secrets.NEON_PROJECT_ID }}" >> .env.development.local
          echo "PARENT_BRANCH_ID=${{ secrets.PARENT_BRANCH_ID }}" >> .env.development.local
          echo "DATABASE_URL=${DATABASE_URL}" >> .env.development.local
          echo "SESSION_SECRET=${SESSION_SECRET}" >> .env.development.local

          docker-compose -f docker-compose.dev.yml --env-file .env.development.local up -d --build
          docker-compose -f docker-compose.dev.yml ps
          docker-compose -f docker-compose.dev.yml logs --tail=50

      - name: Run tests
        run: |
          npm install
          npm test

      - name: Shutdown services
        if: always()
        run: docker-compose -f docker-compose.dev.yml down


  # 🚀 Job 2: Main branch deploy to Cloud Run
  deploy-prod:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
      
      - name: Build image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/acquisition-app:latest .

      - name: Push Docker image to Artifact Registry
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/acquisition-app:latest
